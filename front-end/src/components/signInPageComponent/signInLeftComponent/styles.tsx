import styled from 'styled-components';

const styles = {
  Container: styled.div`
    width: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
  `,
};

export default styles;
